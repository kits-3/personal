package project.management.controlandmanagement;

import project.management.entity.ProductEntity;
import project.management.entity.StoreMachineEntity;

// 잘 하는것 같지만서도 관계를 정의하s 고 역할을 나누는 것이 조금 부족해 보인다...........
public interface IProduct {
	boolean product();
	boolean productOption(String option);
	void insertProduct(ProductEntity productEntity, ProductEntity productAlreadyInsertEntity);
	void insertStoreMachine(StoreMachineEntity storeMachineEntity,StoreMachineEntity storeMachineAlreadyInserEntity);
	void updateProduct(ProductEntity productEntity);
	// 차이가 뭔가요~~~~~~~~~~~
	// this name product is wrong 
	void deleteStoreMachine();
	void deleteProduct();
}
